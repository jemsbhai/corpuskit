name: CorpusKit CodeQL configuration

paths:
  - src
  - apps/web

paths-ignore:
  - docs
  - tests
  - "**/node_modules"
  - "**/.next"

queries:
  - uses: security-extended
  - uses: security-and-quality
